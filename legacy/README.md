# Legacy Integration Sketches

These scripts are kept for historical context. They reference project-specific
modules, local checkpoints, or private configuration files that are not included
in the public repository.

Use the maintained public paths instead:

```bash
python examples/minimal_env.py
python -m unittest discover -s tests
```
